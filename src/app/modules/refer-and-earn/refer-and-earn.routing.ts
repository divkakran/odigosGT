import { Routes } from '@angular/router';

import { ReferAndEarnComponent } from './refer-and-earn.component';

export const routes: Routes = [
    { path: '' , component: ReferAndEarnComponent ,pathMatch:'full'}
]