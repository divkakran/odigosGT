import { Routes } from '@angular/router';
import { EasyBookingComponent } from './easy-booking.component';

export const routes: Routes = [
    {path:'' , component: EasyBookingComponent , pathMatch: 'full'} 
]
