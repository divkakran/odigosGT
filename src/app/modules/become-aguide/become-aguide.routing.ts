import { Routes } from '@angular/router';

import { BecomeAGuideComponent } from './become-aguide.component';

export const routes: Routes = [
    { path: '' , component: BecomeAGuideComponent , pathMatch:'full'}
]