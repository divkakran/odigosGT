import { Routes } from '@angular/router';
import { BookingSuccessfulComponent } from './booking-successful.component';

export const routes: Routes = [
    {path: '' , component: BookingSuccessfulComponent , pathMatch: 'full'}
]