import { Routes } from '@angular/router';
import { BookingFailureComponent } from './booking-failure.component';

export const routes: Routes = [
    {path: '' , component: BookingFailureComponent , pathMatch: 'full'}
]